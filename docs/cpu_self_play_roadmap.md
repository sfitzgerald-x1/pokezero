# CPU Self-Play Roadmap

This document summarizes the current state of the local CPU-based self-play proof of concept and the remaining work needed before it should be treated as an experiment loop rather than scaffolding.

## Goal

The near-term goal is a local, CPU-only Gen 3 random battle loop that can:

- generate teacher-bootstrap and self-play data through the local Showdown harness
- train and resume linear baseline policies without GPU dependencies
- benchmark candidates against fixed baselines and promoted historical checkpoints
- gate checkpoint advancement with auditable promotion criteria
- stop bad long-running jobs through post-iteration audits
- compare completed runs with enough metadata to decide what to try next

This goal is narrower than the eventual transformer/PPO direction. The CPU loop is successful when it can produce reproducible, benchmarked runs that are safe to leave running locally and informative enough to compare.

## Current State

The CPU loop is now mostly wired end to end.

Implemented:

- The local Showdown BattleStream environment can run Gen 3 random battles and expose stable player-relative observations for either side.
- The rollout path records JSONL trajectories with legal masks, selected actions, opponent-action labels, terminal outcomes, capped-game markers, policy identifiers, and collection metrics.
- Dataset loading supports temporal windows and terminal-winner-derived returns, so winning actions are reinforced even when the immediate step reward is zero.
- A dependency-free linear softmax policy can train from behavior-cloning or reward-weighted objectives and save/load versioned checkpoints.
- A scripted Gen 3 randbat teacher exists for bootstrap data. It uses local Showdown dex metadata and first-pass context heuristics for damage preference, status pressure, team-status cures, low-HP preservation switches, status-aware switch penalties, recovery/setup, Spikes, and Rapid Spin.
- Teacher bootstrap can collect train and held-out validation rollouts, run strict-teacher preflight, train a linear checkpoint, benchmark it, and write a manifest.
- Linear self-play can run multiple iterations from random, simple, or checkpoint initial policies with current-policy-only training data, frozen historical opponents, checkpoint warm starts, resume support, validation metrics, and parallel collection workers.
- Neural scaffolding exists behind the optional `neural` extra, including offline transformer training, neural checkpoint benchmarking, and a first neural iteration loop. This is not required for the CPU baseline milestone.
- Promotion gates check benchmark win rates, minimum game counts, incumbent comparisons, capped-game rates, teacher-degradation counters, and Wilson lower-bound no-regression criteria.
- The promotion registry records accepted checkpoints, can copy managed artifacts, verifies registry/checkpoint integrity, feeds promoted opponent pools back into self-play, previews pool selection, advances auto-promoted managed artifacts as the next current policy spec, excludes the current checkpoint by resolved checkpoint identity rather than raw string equality, and supports recoverable retention archiving.
- Run audits can check latest benchmark health, capped-game rates, same-opponent regressions, repeated promotion failures, decision-round length, missing benchmark opponents, and best-effort process RSS high-water marks.
- Run audits can mark selected checks as warning-only diagnostics, so early CPU runs can surface provisional decision-round, same-opponent-regression, or experiment-scoped RSS concerns without stopping when the strict benchmark/capped/coverage gates still pass.
- Rollout benchmarking can compare arbitrary candidate policies against a shared opponent set in mirrored seats, with optional candidate-vs-candidate mirrors. This provides the command shape needed for direct cold-start versus teacher-bootstrap evidence instead of aggregate comparisons with different opponent sets.
- CPU smoke, pilot, and long-run wrappers can generate plans, execute guarded runs, persist wrapper summaries, calibrate audit configs from pilot evidence, replay those configs, launch readiness-checked long runs, and report or compare run health from summaries.
- CPU smoke and pilot execution wrappers now reject non-fresh run roots before launching nested work, so accidental reruns do not overwrite wrapper summaries or collide later with existing bootstrap/self-play artifacts.
- CPU pilot planning and execution now reject calibration benchmark-game floors that the selected `--evaluation-games` cannot guarantee before launching nested work.
- CPU long-run planning can decouple the promotion gate profile from the post-iteration audit source, so a feasible smoke/default gate profile can still enforce the stronger pilot-derived or summary-derived audit config during the run.
- `cpu-readiness-report` can roll up core pilot readiness, long-run derived audit health, and promotion-registry opponent-pool readiness from existing artifacts without launching games.
- Source provenance is recorded in major run artifacts so dirty or unexpected code snapshots are visible during review.
- A read-only Gen 3 randbat belief sidecar and compact belief observation features exist, but the detailed sidecar state is not required for the current linear CPU loop.
- A real local smoke-scale `cpu-pilot-run` has passed against a local Showdown checkout with two seeded pilots, deterministic teacher scenario preflight, rollout-backed `status_pressure` branch gates, audit calibration, and calibrated audit replay. This proves the wrapper path works on local artifacts; it is still too thin to define long-run quality thresholds.
- A stronger local `cpu-pilot-run` has passed with two seeded pilots, six total self-play benchmark iterations, a 50-game calibration sufficiency floor, calibrated audit replay, and `cpu-readiness-report` marking `pilot_suite_ready` as PASS.
- A small standalone scripted-teacher bootstrap checkpoint exists with held-out validation data, source provenance, and fixed-baseline benchmarks against random-legal, simple-legal, and scripted-teacher opponents.
- A first matched-configuration local cold-start versus teacher-bootstrap comparison has completed under the smoke audit profile. It validates the run/report/compare/readiness path for both arms and provides early relative evidence, but it is not yet a calibrated long-run quality threshold.
- A first expanded comparison has exercised `--runtime-audit-config` with summary-derived envelope guardrails. Both arms launched correctly, wrote wrapper summaries, and failed closed under the provisional audit config, which is useful negative evidence for the next calibration pass.
- A promoted-opponent-pool rehearsal has passed with the fixed current-policy exclusion path, a required promoted pool size of 1, auto-promotion into a managed artifact directory, and the existing adjusted audit config. This validates the promotion-registry discipline at smoke scale.
- A larger shared-opponent benchmark now includes fixed baselines, a promoted historical teacher-bootstrap checkpoint, and candidate head-to-head at 120 games per pairwise row. It provides better comparison evidence but still does not identify a clear incumbent.
- A matched promotion-disciplined cold-start versus teacher-bootstrap comparison has passed with each arm launched from its latest promoted checkpoint, selecting only its prior promoted checkpoint as the historical opponent, auto-promoting into a managed artifact directory, and satisfying the required promoted-pool preflight.
- A direct shared-opponent benchmark now covers the newly promoted promotion-disciplined checkpoints against fixed baselines, launch incumbents, older promoted historical checkpoints, and candidate head-to-head rows. It is useful exploratory evidence but is not large enough to decide an incumbent.
- Targeted 120-game benchmark rows now cover the decision-relevant pairs from the newly promoted shared benchmark. The newly promoted checkpoints did not clearly beat each other, and neither newly promoted checkpoint cleared its relevant historical reference.
- Post-iteration audit enforcement now runs before auto-promotion when latest promotion is optional, so hard audit failures no longer write new promotion records before stopping the run.
- A short reference continuation from `expanded-teacher-bootstrap-linear-iter-0001` has completed with RSS marked warning-only for that experiment. It produced a promoted checkpoint under the smoke gate and preserved benchmark/capped-game health, but that promotion is procedural rather than strength evidence: the direct row against the launch reference was exactly tied at 30/60, so it is not yet evidence that continuation improved the strongest observed teacher-bootstrap reference.
- A targeted 120-game-per-row benchmark of the RSS-warning continuation checkpoint did not clear the launch reference. It beat fixed baselines, tied the promotion-disciplined teacher checkpoint, and lost 57/120 versus 63/120 against `expanded-teacher-bootstrap-linear-iter-0001`, so this continuation checkpoint should not be treated as the next incumbent.

## What Is Left

The remaining work is less about wiring and more about making the loop empirically usable.

1. Continue calibrating hard-stop versus diagnostic guardrails from real expanded runs.

   The first summary-based envelope config was useful because it stopped runs that violated explicit health gates, but it was calibrated from only two smoke-profile summaries. Warning-only audit checks now let decision-round ceilings and same-opponent regression remain visible diagnostics while benchmark win-rate, capped-game, benchmark coverage, promotion-failure, and RSS checks stay eligible as hard stops. More evidence is still needed before these warning boundaries should be promoted back into hard failures. Validation fit remains imitation-health only.

2. Settle the RSS guardrail before longer unattended continuations.

   Two guarded continuations from `expanded-teacher-bootstrap-linear-iter-0001` launched correctly, used a promoted historical opponent pool, cleared the benchmark/capped-game/promotion-pool checks, and failed only on the hard RSS ceiling around 697-700 MB. A follow-up short continuation made `latest_process_peak_rss_mb` warning-only in an experiment-scoped config and passed at 708.58 MB, which is higher than both hard-stop failures. This unblocks short diagnostic runs, but longer unattended runs still need a deliberate RSS policy: raise/recalibrate the local ceiling from newer evidence, reduce run memory, or keep RSS warning-only only for explicitly bounded experiments. Promotion records should remain blocked when hard post-iteration audit checks fail.

3. Validate scripted-teacher data quality with measurements.

   The teacher now has multiple tactical heuristics, but heuristic branches firing is not the same as better bootstrap data. The next useful evidence is teacher-vs-baseline and teacher-self-play benchmark deltas, branch coverage, degradation counters, and capped-game health.

4. Decide the first local success threshold.

   Before treating any CPU run as progress, define provisional minimums for benchmark game count, fixed-baseline win rate, incumbent win rate, capped-game rate, decision-round limits, throughput, RSS ceiling, and required promoted-opponent-pool size. These should come from pilot calibration plus manual judgment, not from arbitrary defaults.

5. Keep the neural path optional until the CPU baseline is trustworthy.

   The neural scaffold can run CPU smoke tests when `.[neural]` is installed, but it is not the blocking path for the local CPU proof of concept. PPO, GPU training, and distributed rollout orchestration remain later milestones.

## Suggested Next Tasks

The next implementation tasks should be chosen in this order unless a real pilot run exposes a more urgent failure.

1. Pivot away from additional reference-continuation iterations for now, because the RSS-warning continuation did not clear `expanded-teacher-bootstrap-linear-iter-0001` in the targeted benchmark.
2. Decide the durable RSS policy for larger local CPU runs: recalibrate the ceiling from newer evidence, reduce policy/run memory, or keep RSS warning-only only for bounded diagnostics.
3. Add measured scripted-teacher data-quality work next, using deterministic scenarios, rollout-backed branch coverage, teacher-vs-baseline deltas, and capped-game health rather than heuristic edits alone.

## Progress Updates

- Added `cpu-readiness-report` so the core pilot, long-run, and promotion readiness artifacts can be evaluated in one read-only command. This closed the immediate reporting gap for existing artifacts and made the first local pilot inspection straightforward.
- Ran a real local smoke-scale CPU pilot suite at `runs/cpu-pilots-local-20260622-smoke-2` against `/Users/scott/workspace/pokerena/vendor/pokemon-showdown`. The suite passed in 138.6 seconds with two seeded pilots, deterministic teacher scenario preflights passing 13/13 scenarios per pilot, rollout-backed `status_pressure` branch gates passing with 13 aggregate observations, zero capped games in the nested smoke self-play runs, generated audit calibration, and calibrated audit replay. `cpu-pilot-report --require-ready --require-smoke-ready --require-calibration-run-count 2 --require-calibration-benchmark-iterations 4 --require-calibration-min-benchmark-games 1` passed. `cpu-readiness-report --pilot-summary ...` reports the pilot item as PASS while the overall checklist remains not ready because no long-run summary or promotion registry was supplied.
- The local pilot validation exposed a stale-run-root failure mode when rerunning into an existing ignored artifact directory. The smoke and pilot execution wrappers now fail fast on non-fresh run roots before starting child commands.
- Ran a stronger local pilot attempt at `runs/cpu-pilots-local-20260622-strong-1` with two seeded pilots, three self-play iterations per pilot, stricter teacher branch coverage, and a 50-game calibration floor. Both nested pilots completed successfully, but suite-level calibration failed after about 711 seconds because the selected `--evaluation-games 8` produced a 48-game observed floor. A manual compare at a 48-game floor succeeded and wrote a provisional config, but that artifact should not become the long-run config. The failure is now captured as a preflight validation gap: pilot plan/run reject calibration floors that cannot be guaranteed by the selected evaluation-game count before launching nested work. For a 50-game floor the current guaranteed recommendation is `--evaluation-games 13`; this is intentionally conservative because it relies on the minimum four post-iteration benchmark matchups rather than incidental extra reference matchups.
- Reran the stronger local pilot at `runs/cpu-pilots-local-20260622-strong-2` with `--evaluation-games 13`, which guarantees 52 benchmark games for the requested 50-game calibration floor. The suite passed in 976.5 seconds with two seeded pilots, six total self-play benchmark iterations, deterministic teacher scenario preflights passing 13/13 scenarios per pilot, rollout-backed `status_pressure` branch gates passing with 37 aggregate observations, zero capped games, generated audit calibration, and calibrated audit replay. `cpu-pilot-report --require-ready --require-smoke-ready --require-calibration-run-count 2 --require-calibration-benchmark-iterations 6 --require-calibration-min-benchmark-games 50` passed. The generated pilot audit config has `min_latest_benchmark_games: 78`, `min_latest_benchmark_win_rate: 0.475962`, and recommends `--evaluation-games 20` for post-iteration audit use. At that point, `cpu-readiness-report --pilot-summary ...` reported `pilot_suite_ready` as PASS while the overall checklist remained blocked on the long-run summary and promotion registry inputs.
- Ran a standalone scripted-teacher bootstrap at `runs/scripted-teacher-bootstrap-local-20260622-medium-1` with 64 train games, 16 held-out validation games, 16 benchmark games per direction, four workers, `window-size 4`, `feature-count 131072`, deterministic seed bands, and local Showdown. It wrote `linear-bootstrap.json`, train/validation rollout JSONL, and `manifest.json` with source provenance. The 96-game fixed-baseline benchmark had zero capped games and measured 32-game head-to-head aggregate linear-bootstrap win rates of 0.8125 vs random-legal, 0.65625 vs simple-legal, and 0.15625 vs scripted-teacher. Held-out teacher imitation accuracy was 0.3767; this remains an imitation-health metric, not a policy-strength signal.
- Ran a matched-configuration local smoke-profile comparison at `runs/cpu-comparison-local-20260622` using two iterations, 16 collection games per iteration, four workers, `--evaluation-games 20`, `window-size 4`, `feature-count 131072`, and shared seed bands. The cold-start arm completed in 426.8 seconds with two recorded promotions, latest benchmark win rate 0.5583 over 120 latest benchmark games, latest benchmark capped rate 0.0167, latest average decision rounds 68.19, benchmark average decision rounds 68.60, and peak RSS 443.11 MB. The teacher-bootstrap arm started from `runs/scripted-teacher-bootstrap-local-20260622-medium-1/linear-bootstrap.json`, completed in 439.6 seconds with two recorded promotions, latest benchmark win rate 0.5875 over 160 latest benchmark games, zero latest benchmark capped games, latest average decision rounds 60.00, benchmark average decision rounds 58.89, and peak RSS 578.66 MB. The aggregate benchmark win rates are not directly comparable because the teacher-bootstrap arm also benchmarked against the static `linear-bootstrap` reference, producing four opponent groups versus three for cold-start; on the shared `random-legal` and `simple-legal` baselines, teacher-bootstrap scored 0.80 and 0.60 versus cold-start at 0.625 and 0.50. `cpu-long-run-report --require-derived-audit`, `cpu-long-run-compare --fail-on-non-passing`, and `cpu-readiness-report --require-ready` all passed for these artifacts. Because the runtime audit source was the smoke profile rather than the pilot-derived audit config, this is path validation plus early relative evidence, not final long-run strength evidence.
- Calibrated provisional long-run thresholds from the two completed comparison summaries with `cpu-long-run-calibrate --aggregate-mode envelope --require-run-count 2 --require-benchmark-iterations 2 --require-min-benchmark-games 100`. The envelope config is intentionally conservative while evidence is thin: `min_latest_benchmark_win_rate: 0.5025`, `min_latest_benchmark_games: 120`, capped-rate ceilings of 0.10 for collection and benchmark, `max_latest_average_decision_rounds: 75.00625`, `max_latest_benchmark_average_decision_rounds: 75.46`, `max_latest_process_peak_rss_mb: 636.521875`, `max_benchmark_win_rate_drop: 0.05`, `max_consecutive_promotion_failures: 1`, benchmark required, fixed-baseline opponent coverage required, and latest promotion optional. The generated post-iteration recommendation is `--evaluation-games 30`. Median calibration was not selected because it would set an RSS ceiling below the teacher-bootstrap observed peak and would not keep both early arms passable.
- Ran the first expanded comparison at `runs/cpu-comparison-local-20260623-expanded` with two requested iterations, 32 collection games per iteration, four workers, `--evaluation-games 30`, `window-size 4`, `feature-count 131072`, shared seed bands, `--profile smoke`, and `--runtime-audit-config runs/cpu-comparison-local-20260622/envelope-audit-config.json`. This verified the summary-derived runtime-audit path in a real wrapper launch, but both arms failed closed under the provisional guardrails. The cold-start arm stopped after iteration 1 with latest benchmark win rate 0.5667 over 120 games, zero capped games, peak RSS 315.78 MB, and failed checks `latest_average_decision_rounds` at 80.71875 versus 75.00625 and `latest_benchmark_average_decision_rounds` at 75.8583 versus 75.46. The teacher-bootstrap arm reached iteration 2 with latest benchmark win rate 0.525 over 240 games, best benchmark win rate 0.6056, zero capped games, latest average decision rounds 66.28125, latest benchmark average decision rounds 62.925, peak RSS 619.53 MB, and failed only `benchmark_win_rate_drop_by_opponent` at 0.10 versus 0.05. Teacher-bootstrap iteration 2 still promoted under the smoke promotion gate, and `cpu-readiness-report` confirmed the promotion registry pool was readable, but the wrapper correctly reported the long-run derived audit as not ready.
- Added warning-only audit checks and reran the expanded comparison at `runs/cpu-comparison-local-20260623-warning-audit` with the same two-iteration, 32-game, four-worker, `--evaluation-games 30`, shared-seed settings. The adjusted config kept the prior envelope thresholds but marked `latest_average_decision_rounds`, `latest_benchmark_average_decision_rounds`, and `benchmark_win_rate_drop_by_opponent` as warning-only. Both arms passed the wrapper and derived audit. Cold-start completed in 632.6 seconds with two promotions, latest benchmark win rate 0.6611 over 180 games, zero capped games, latest average decision rounds 70.75, latest benchmark average decision rounds 64.17, peak RSS 490.80 MB, and no final warning checks; its first iteration still tripped the decision-round warning at 80.71875, which is the useful evidence that this check should remain diagnostic for now. Teacher-bootstrap completed in 723.3 seconds with two promotions, latest benchmark win rate 0.525 over 240 games, best benchmark win rate 0.6056, zero capped games, latest average decision rounds 66.28, latest benchmark average decision rounds 62.93, peak RSS 630.09 MB against the 636.52 MB strict ceiling, and warning-only `benchmark_win_rate_drop_by_opponent` at 0.10 versus 0.05. The aggregate win rates are not direct strength comparisons because the benchmark opponent sets differ; on shared fixed baselines, both arms scored 0.8333 versus `random-legal`, while cold-start scored 0.5000 versus `simple-legal` and teacher-bootstrap scored 0.4500. `cpu-long-run-compare --fail-on-non-passing --refresh-derived-audit` passed for both summaries, and `cpu-readiness-report --require-ready --refresh-derived-audit` passed using the teacher-bootstrap summary and promotion registry.
- Ran a direct shared-opponent benchmark at `runs/shared-policy-benchmark-local-20260623/cold-vs-teacher-shared-opponents.json` using `rollout_cli benchmark`, the latest cold-start and teacher-bootstrap checkpoints from the adjusted comparison, 30 games per seat orientation, shared seed start `9200001`, shared `random-legal` and `simple-legal` opponents, and candidate head-to-head enabled. The run completed 300 total games in 428.2 seconds, with zero capped games, average decision rounds 62.71, 0.701 games/s, 43.94 decisions/s, and peak RSS 401.09 MB. Against fixed baselines, cold-start scored 47/60 versus `random-legal` and 33/60 versus `simple-legal`; teacher-bootstrap scored 45/60 versus `random-legal` and 32/60 versus `simple-legal` with two ties. In direct candidate head-to-head, teacher-bootstrap beat cold-start 35/60 versus 25/60. This is useful comparable evidence, but the pairwise samples are only 60 games each, below the provisional 120-game benchmark floor, and no axis clearly distinguishes the two checkpoints. It should not by itself decide the next incumbent.
- Started a promoted-pool rehearsal from the teacher-bootstrap promotion registry with a required promoted historical opponent pool. The guarded run stopped on `latest_process_peak_rss_mb` at 645.66 MB versus the provisional 636.52 MB ceiling, and the manifest exposed a more important selector bug: the current collector checkpoint was included in the promoted opponent pool when launched with a relative `linear:` spec while the registry selected the same checkpoint as an absolute path. Current-policy exclusion now compares resolved checkpoint identity for `linear:` and `neural:` specs across runtime selection, registry preview, and run-audit checks, and auto-promotion now advances managed artifact copies as the next current policy spec so the just-promoted copy is not selected as a historical opponent. A registry preview against the copied teacher-bootstrap registry now selects only the prior promoted checkpoint and marks the latest/current checkpoint as `excluded_current_policy`.
- Reran the promoted-pool rehearsal at `runs/promotion-pool-rerun-local-20260623/teacher-bootstrap-run` after fixing current-policy exclusion. The guarded wrapper passed in 310.91 seconds with `--require-promoted-opponent-pool-size 1`, a copied teacher-bootstrap promotion registry, managed auto-promotion artifacts, and the adjusted audit config. The collection phase ran 16 games with zero capped games, average decision rounds 65.81, 50.33 decisions/s, and peak RSS 553.42 MB versus the 636.52 MB hard ceiling. The recorded opponent pool contained `random-legal`, `simple-legal`, and only the prior promoted teacher-bootstrap checkpoint; it excluded the current/latest teacher-bootstrap checkpoint. The derived audit passed with latest benchmark win rate 0.5333 over 180 games, zero benchmark capped games, benchmark average decision rounds 64.34, and promoted-pool requirement observed at 1.0. The promotion gate recorded `promoted-pool-rehearsal-linear-iter-0001` with fixed-baseline benchmark win rate 0.5583 over 120 games and incumbent win rate 0.4833 over 60 games. This is promotion-registry path validation, not final strength evidence.
- Ran a larger direct shared-opponent benchmark at `runs/shared-policy-benchmark-local-20260623/promoted-history-120-pairwise/cold-vs-teacher-promoted-history.json` using the latest cold-start and teacher-bootstrap checkpoints from the adjusted comparison, 60 games per seat orientation, shared seed start `9700001`, shared `random-legal` and `simple-legal` opponents, the prior promoted teacher-bootstrap checkpoint as a historical opponent, and candidate head-to-head enabled. The run completed 840 games in 1295.24 seconds, with zero capped games, average decision rounds 64.38, 0.649 games/s, 41.75 decisions/s, and peak RSS 482.625 MB. Every pairwise row has 120 games. Cold-start scored 102/120 versus `random-legal`, 65/120 versus `simple-legal`, and 57/120 versus the promoted historical teacher checkpoint. Teacher-bootstrap iteration 2 scored 91/120 versus `random-legal` with one tie, 61/120 versus `simple-legal`, and 51/120 versus the promoted historical teacher checkpoint with two ties. Teacher-bootstrap iteration 2 beat cold-start head-to-head 66/120 versus 54/120. The prior promoted teacher checkpoint beat both current candidates directly in this sample, but the head-to-head margins are still within sampling noise, so this evidence improves comparability without identifying a clean incumbent.
- Ran a matched promotion-disciplined comparison under `runs/promotion-disciplined-comparison-local-20260623` with one guarded iteration per arm, 16 collection games, four workers, `--evaluation-games 30`, shared seed bands, the adjusted audit config, copied per-arm promotion registries, managed artifact dirs, and `--require-promoted-opponent-pool-size 1`. The cold-start arm launched from `expanded-cold-start-linear-iter-0002`, selected `random-legal`, `simple-legal`, and only `expanded-cold-start-linear-iter-0001` as opponents, passed in 281.83 seconds, recorded `promotion-disciplined-cold-linear-iter-0001`, and passed derived audit with latest benchmark win rate 0.5944 over 180 games, zero benchmark capped games, collection average decision rounds 68.31, benchmark average decision rounds 63.85, and peak RSS 529.20 MB. Its promotion gate fixed-baseline benchmark was 0.6083 over 120 games, with incumbent win rate 0.5667 over 60 games. The teacher-bootstrap arm launched from `expanded-teacher-bootstrap-linear-iter-0002`, selected `random-legal`, `simple-legal`, and only `expanded-teacher-bootstrap-linear-iter-0001` as opponents, passed in 283.09 seconds, recorded `promotion-disciplined-teacher-linear-iter-0001`, and passed derived audit with latest benchmark win rate 0.6056 over 180 games, zero benchmark capped games, collection average decision rounds 64.75, benchmark average decision rounds 68.02, and peak RSS 539.88 MB. Its promotion gate fixed-baseline benchmark was 0.6667 over 120 games, with incumbent win rate 0.4833 over 60 games. `cpu-long-run-compare --fail-on-non-passing --refresh-derived-audit` passed for both summaries. Under the smoke-profile gate used here, promotion records path health and registry plumbing rather than proving incumbent strength. This validates the promotion-disciplined training path for both arms, but the newly promoted checkpoints still need direct shared-opponent benchmarking before choosing an incumbent.
- Ran a direct shared-opponent benchmark for the newly promoted promotion-disciplined checkpoints at `runs/shared-policy-benchmark-local-20260623/new-promoted-60-pairwise/new-promoted-cold-vs-teacher.json`. The run used 30 games per seat orientation, shared seed start `10100001`, fixed `random-legal` and `simple-legal` opponents, both cold-start historical checkpoints, both teacher-bootstrap historical checkpoints, and candidate head-to-head enabled. It completed 780 games in 1167.59 seconds, with one capped game, average decision rounds 63.96, 0.668 games/s, 42.73 decisions/s, and peak RSS 846.234375 MB. Each pairwise row has 60 games. The new cold-start checkpoint scored 46/60 versus `random-legal`, 31/60 versus `simple-legal`, 38/60 versus `expanded-cold-start-linear-iter-0001`, 24/60 versus `expanded-cold-start-linear-iter-0002`, 24/60 versus `expanded-teacher-bootstrap-linear-iter-0001`, and 27/60 versus `expanded-teacher-bootstrap-linear-iter-0002`. The new teacher-bootstrap checkpoint scored 45/60 versus `random-legal`, 26/60 versus `simple-legal`, 36/60 versus `expanded-cold-start-linear-iter-0001`, 31/60 versus `expanded-cold-start-linear-iter-0002` with one capped game, 27/60 versus `expanded-teacher-bootstrap-linear-iter-0001`, and 32/60 versus `expanded-teacher-bootstrap-linear-iter-0002`. New teacher-bootstrap beat new cold-start head-to-head 35/60 versus 24/60 with one tie. The point estimates favor new teacher-bootstrap head-to-head, favor new cold-start on the fixed simple baseline, and show new cold-start weaker than its launch incumbent in this sample. Because rows are only 60 games, one capped game appeared, and benchmark-only RSS was materially higher than the guarded training runs, this should drive targeted follow-up benchmarks and resource monitoring rather than an incumbent decision.
- Ran targeted higher-sample rows under `runs/targeted-incumbent-benchmarks-local-20260623` with 60 games per seat orientation for four decision-relevant pairs. Across 480 games, the targeted rows had zero capped games, average decision rounds 67.73, 0.655 games/s, 44.37 decisions/s, and peak RSS 340.046875 MB. New teacher-bootstrap versus new cold-start finished 60/120 versus 59/120 with one tie. New cold-start versus `expanded-cold-start-linear-iter-0002` finished 54/120 versus 66/120. New teacher-bootstrap versus `expanded-teacher-bootstrap-linear-iter-0001` finished 40/120 versus 79/120 with one tie. New teacher-bootstrap versus `expanded-teacher-bootstrap-linear-iter-0002` finished 58/120 versus 61/120 with one tie. This removes the broad benchmark's apparent new-teacher head-to-head edge, confirms that new cold-start did not clear its launch incumbent, and shows that the latest guarded teacher iteration did not improve over its launch point. Combined with the earlier promoted-history rows, `expanded-teacher-bootstrap-linear-iter-0001` remains the strongest observed reference for the next continuation.
- Attempted a guarded continuation from `expanded-teacher-bootstrap-linear-iter-0001` under `runs/reference-continuation-local-20260623/teacher-bootstrap-from-iter1`. The first run launched correctly with a copied promotion registry, required promoted-opponent pool size 1, adjusted runtime audit config, and 16 collection games, but failed the wrapper on `latest_process_peak_rss_mb`: 696.90625 MB observed versus the 636.521875 MB hard ceiling. All other runtime audit checks passed: promoted opponent pool observed 2.0, latest benchmark win rate 0.5792 over 240 games, zero collection capped games, zero benchmark capped games, collection average decision rounds 45.69, and benchmark average decision rounds 59.28. That run exposed that auto-promotion was recorded before the hard post-iteration audit failure. The ordering is now fixed, and the audit-guard rerun at `runs/reference-continuation-local-20260623/teacher-bootstrap-from-iter1-audit-guard` reproduced the RSS failure at 700.46875 MB while leaving the copied registry at its original three entries, writing no promoted checkpoint artifact, and leaving the iteration manifest `promotion` field empty. The benchmark rows in the audit-guard rerun were 49/60 versus `random-legal`, 33/60 versus `simple-legal`, 29/60 versus `promotion-disciplined-teacher-linear-iter-0001` with one tie, and 28/60 versus the launch reference `expanded-teacher-bootstrap-linear-iter-0001`.
- Reran the reference continuation under `runs/reference-continuation-local-20260623/teacher-bootstrap-from-iter1-rss-warning` with an experiment-scoped copy of the adjusted audit config that added `latest_process_peak_rss_mb` to `warning_check_names`. The wrapper passed in 359.84 seconds, recorded `audit_warning_checks: latest_process_peak_rss_mb`, promoted `reference-continuation-rss-warning-linear-iter-0001`, and wrote the managed checkpoint at `runs/reference-continuation-local-20260623/teacher-bootstrap-from-iter1-rss-warning/promoted-checkpoints/000004-reference-continuation-rss-warning-linear-iter-0001.json`. The derived audit passed with latest benchmark win rate 0.5583 over 240 games, zero collection capped games, zero benchmark capped games, collection average decision rounds 51.25, benchmark average decision rounds 62.91, and peak RSS 708.58 MB against the old 636.52 MB warning threshold. That RSS peak is above both prior RSS-only hard-stop runs, so warning-only RSS should be treated as diagnostic evidence rather than proof that the current memory profile is acceptable. Benchmark rows were 49/60 versus `random-legal`, 24/60 versus `simple-legal` with one tie, 31/60 versus `promotion-disciplined-teacher-linear-iter-0001`, and 30/60 versus the launch reference `expanded-teacher-bootstrap-linear-iter-0001`. This shows the short continuation can run with RSS diagnostic-only, but it does not yet show improvement over the launch reference.
- Ran the targeted follow-up benchmark for `reference-continuation-rss-warning-linear-iter-0001` at `runs/targeted-incumbent-benchmarks-local-20260623/rss-warning-continuation/continuation-targeted-120-pairwise.json`. The run used 60 games per seat orientation, shared seed start `10700001`, fixed `random-legal` and `simple-legal` opponents, `promotion-disciplined-teacher-linear-iter-0001`, and the launch reference `expanded-teacher-bootstrap-linear-iter-0001`. It completed 480 games in 704.62 seconds, with zero capped games, average decision rounds 66.44, 0.681 games/s, 45.26 decisions/s, and peak RSS 499.95 MB. The continuation checkpoint scored 92/120 versus `random-legal`, 67/120 versus `simple-legal`, tied `promotion-disciplined-teacher-linear-iter-0001` at 60/120, and lost to `expanded-teacher-bootstrap-linear-iter-0001` by 57/120 versus 63/120. This higher-sample row confirms the RSS-warning continuation checkpoint should not replace the launch reference.

## Out Of Scope For This Milestone

- GPU training.
- PPO-style actor-critic updates.
- Distributed self-play orchestration.
- Raw Showdown replay corpus discovery and conversion.
- Treating validation accuracy as a policy-strength signal.
- Permanent deletion of old checkpoint artifacts.

## Readiness Checklist

The local CPU loop is ready to be used as a serious prototype when all of these are true:

- A strict teacher scenario preflight passes.
- A rollout-backed teacher benchmark passes required branch and degradation gates.
- A `cpu-pilot-run` passes with at least two seeded pilots and writes a calibrated audit config from sufficient evidence.
- `cpu-pilot-report --require-ready --require-smoke-ready` passes under the selected sufficiency floors.
- A teacher-bootstrap checkpoint exists with held-out validation, fixed-baseline benchmarks, and clean or intentionally dirty source provenance.
- A guarded `cpu-long-run-run` launches from the ready pilot artifact and completes without post-iteration audit failure.
- `cpu-long-run-report --require-derived-audit` passes for the completed long run.
- `cpu-long-run-compare --fail-on-non-passing` can compare cold-start and teacher-bootstrap runs without load errors.
- A promotion registry preflight verifies the selected promoted opponent pool before any longer unattended run relies on it.
- The selected non-smoke long-run thresholds are explicit enough that a passing run means more than plumbing health.
