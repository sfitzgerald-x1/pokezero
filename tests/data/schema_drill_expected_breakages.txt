# Class-(iii) tests: their SUBJECT is the default's identity, so rotating the default MUST
# break them. This file is the drill's pass condition -- the breakage set must equal it exactly.
#
# A test belongs here only if it asserts *which schema is currently default* -- AND NOTHING ELSE.
# Two entries used to also assert tuples the drill mutates (SUPPORTED, FEATURE_PACK), so they
# broke on the mutation whether or not their default assertion bound, and the
# EXPECTED-BUT-DID-NOT-BREAK detector -- the only guard against a pin silently going stale -- could
# never fire for them. Both were split; a row here must be a single-assertion identity pin. A test that
# merely uses the default is NOT class (iii); it is an unmigrated site and belongs in the
# ledger instead. Keeping that line sharp is the whole point: this file is the one place the
# migration could be silently declared finished by relabelling failures as expected.
test_observation.py::ObservationSpecTest::test_the_fresh_artifact_default_is_v2_2
test_observation_spec_v3.py::SchemaTableTest::test_v2_2_IS_the_default_not_v3
test_observation_spec_v4.py::V4SchemaTableTest::test_v2_2_IS_the_default_not_v4
test_turn_merged_encode.py::SchemaTableTest::test_v2_2_IS_the_default
test_observation_spec_v2_1.py::SpecTableTest::test_v2_2_IS_the_fresh_default
# Fingerprint stamping: the payload HASHES the observation schema, so the fingerprint must move
# when the default does. A fingerprint that survived a schema rotation would be the bug.
#
# This row is INDIRECT and it is the one MIXED row here: the scan reports 1 of its 12 assertions
# as a default read. It is NOT splittable into a clean identity pin the way the five above were.
# Its subject is "the fingerprint tracks whatever the schema is", so the default read and the
# fingerprint assertions are the same claim -- extract the default read and the remaining test
# still breaks under a rotation, because the fingerprint it recomputes still moved. The dead-pin
# detector is therefore structurally blind to this row, and splitting cannot fix that; only
# deleting the row's justification could. Recorded as a KNOWN LIMIT of the rubric rather than
# silently carried: if this row's default read were removed tomorrow, this file would not notice.
test_linear_policy.py::LinearPolicyTest::test_linear_feature_fingerprint_payload_tracks_extractor_source_and_schemas
# The committed token-format dump carries `dual_schema_story.current_default`, whose documented
# purpose is "derived from pokezero.observation.OBSERVATION_SCHEMA_VERSION at extraction time".
# An artifact whose job is to RECORD the default necessarily changes when the default changes;
# a dump that survived a rotation would be stale, which is the defect this test exists to catch.
# The encode in that extractor is pinned to V2_2_REPLAY_OBSERVATION_SPEC and does not move --
# only this one documentation line does, which is why it qualifies as class (iii) rather than
# as an unmigrated site.
#
# Also INDIRECT, and invisible to schema_default_assertion_scan.py: this test asserts a
# byte-for-byte dump comparison, so no assertion of its own names the default -- the default is
# read inside the regeneration it calls. That is why the scan's 5 PINs cover the five direct rows
# and not this one, and why the scan is a completeness check on the identity half of this rubric
# only. The indirect half is held by argument, recorded above each row, not by the tool.
test_token_format_doc.py::TokenFormatDocSelfValidationTest::test_committed_dump_matches_live_regeneration_byte_for_byte

# D2, closed for the five DIRECT identity rows; the two INDIRECT rows are argued separately
# below. Each of the five now names a test that reads the process default and NOTHING else, which
# is what lets "expected but did not break" fire for it -- a pin sharing a test with membership
# assertions breaks under a rotation for either reason, so deleting the pin leaves the test still
# breaking and the dead-pin detector blind.
#
# The task described two such rows. Measured, not assumed, there were FIVE. Derived by AST over
# all 253 test files. Re-derive both buckets with:
#   python scripts/schema_default_assertion_scan.py --texts
#   python scripts/schema_default_assertion_scan.py --self-test    # proves the matcher binds
# Denominator: 253 test files, 12 tests whose assertions read the default -- PIN 5, MIXED 7.
#
# That 12 corrects an earlier 45 of mine. The first scan matched the global's name as a SUBSTRING,
# so it also counted `OBSERVATION_SCHEMA_VERSION_V2_2` (a specific version, immutable) and
# `SUPPORTED_OBSERVATION_SCHEMA_VERSIONS` (a table, not the default) as reads of the mutable
# default. 45 was the count of tests mentioning a schema-ish name; 12 is the count that reads the
# default. The scan now matches bare Names on the AST and self-tests both near-misses.
#
# The 5 PINs the tool reports are exactly the five rows above -- that agreement is the check that
# this rubric's identity half is complete, not merely non-empty. Of the 7 MIXED, six are NOT rows
# here: they read the default to BUILD something rather than to assert its identity, which makes
# them unmigrated sites owned by the ledger's census, not class (iii). The seventh IS a row here
# (the fingerprint payload, 1 of 12 assertions) and is discussed at that row.
# The five mixed tests, all now split into a clean pin plus a residual that holds no default read:
#   test_observation_spec_v3.py    test_v3_is_supported_but_not_the_default
#   test_turn_merged_encode.py     test_v2_2_is_a_supported_schema_entry_and_the_default
#   test_observation_spec_v2_1.py  test_schema_keyed_censuses          (carried TWO default reads)
#   test_observation.py            test_schema_version_is_v2_2_with_..._and_v1_legacy
#   test_observation_spec_v4.py    test_v4_is_supported_..._but_not_the_default
# I first judged `test_schema_keyed_censuses` to be "a different split with a different risk" and
# deferred it; it was the same split. Deferring on a guess was the error, not the judgement.
#
# The last two rows previously named `test_the_fresh_artifact_default_is_v4` and
# `test_v4_IS_the_default` -- methods that exist NOWHERE under tests/, because this rubric was
# first written against the v4-default tree. That also made the file self-contradictory: it
# asserted both `test_v4_IS_the_default` and `test_v2_2_is_a_supported_schema_entry_and_the_default`.
# Both are re-derived above against the tree the drill actually runs on, where
# `OBSERVATION_SCHEMA_VERSION = OBSERVATION_SCHEMA_VERSION_V2_2`, so the contradiction is gone.
#
# Every identity pin is asserted POSITIVELY (default IS v2.2). The negative form ("v4 is not the
# default") SURVIVES a rotation -- under the drill's rotation to v5-drill, v4 is still not the
# default -- so it never breaks, and a non-breaking row in this rubric is a false pin that would
# make the drill report a missing breakage forever. `test_v2_2_IS_the_default_not_v3` was renamed
# from `test_v3_is_not_the_default` for exactly this reason: its body already asserted the
# positive form, and the old name described the implication rather than the assertion.
#
# All five kill-confirmed by rotating the default in src/pokezero/observation.py and running each
# pin and its residual: every pin exit=1, every residual exit=0. A residual that still went red
# would mean the split had not actually removed the default read.
